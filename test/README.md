# 提醒功能测试

本目录包含提醒功能的测试脚本，特别是公开/私有提醒功能的测试。

## 测试脚本

### 1. test_reminder_quick.py
快速测试脚本，验证核心功能。

**运行方式:**
```bash
cd /Users/xiaohu/projects/km-agent_2/test
python3 test_reminder_quick.py
```

**测试内容:**
- 创建公开提醒
- 创建私有提醒
- 查询提醒（公开 + 用户私有）
- 切换公开/私有状态
- 验证数量限制（公开最多10个，私有每用户最多5个）

### 2. test_reminder_visibility.py
完整测试脚本，包含详细的测试场景。

**运行方式:**
```bash
cd /Users/xiaohu/projects/km-agent_2/test
python3 test_reminder_visibility.py
```

**测试场景:**
1. 创建公开提醒（最多10个）
2. 创建私有提醒（每个用户最多5个）
3. 查询提醒（公开 + 用户私有）
4. 切换公开/私有状态
5. 验证数量限制

## 功能说明

### 公开提醒
- 所有用户可见
- 最多10个
- `is_public = 1`, `user_id = NULL`

### 私有提醒
- 仅创建者可见
- 每个用户最多5个
- `is_public = 0`, `user_id = <用户ID>`

### 查询逻辑
- 不带 `user_id`: 只返回公开提醒
- 带 `user_id`: 返回所有公开提醒 + 该用户的私有提醒

## API 端点

### GET /api/reminders
获取提醒列表

**Query Parameters:**
- `user_id` (可选): 用户ID

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "content": "提醒内容",
      "is_public": 1,
      "user_id": null,
      "created_at": "2025-12-04 11:00:00",
      "updated_at": "2025-12-04 11:00:00"
    }
  ]
}
```

### POST /api/reminders
创建提醒

**Request Body:**
```json
{
  "content": "提醒内容",
  "is_public": true,  // 可选，默认true
  "user_id": "user123"  // 可选，私有提醒时必填
}
```

### PUT /api/reminders/:id
更新提醒

**Request Body:**
```json
{
  "content": "新内容",  // 可选
  "is_public": false,  // 可选
  "user_id": "user123"  // 可选，切换为私有时必填
}
```

### DELETE /api/reminders/:id
删除提醒

## 前端使用

在 `ReminderItem.jsx` 中，hover时会显示公开/私有切换按钮：
- 🌐 公开 - 点击切换为私有
- 🔒 私有 - 点击切换为公开

## 注意事项

1. 确保后端服务运行在 `http://localhost:8080`
2. 测试脚本会自动清理创建的测试数据
3. 如果测试失败，请检查数据库连接和表结构
